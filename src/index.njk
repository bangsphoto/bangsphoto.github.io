---
layout: layout.njk
title: bangsphoto | home
active: none
---
<div class="uk-grid-small uk-child-width-1-1@s uk-child-width-1-2@m" uk-grid>
    {% for image in index.images %}
    <a href="{{ image.href }}">
        <img src="{{ image.path }}" alt="{{ image.caption }}" width="{{ image.width }}" height="{{ image.height }}" />
    </a>
    {% endfor %}
</div>
